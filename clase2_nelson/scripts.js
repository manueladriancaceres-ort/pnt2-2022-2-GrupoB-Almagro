console.log('Hola mundo');

const miObjeto = {
    codigo: 100,
    descripcion: 'mesa'
}

console.log(miObjeto);

function calcular(a,b) {
    return a + b;
}

console.log(calcular(100, 50));

const calcularSuma = (a,b) => {
    return a + b;
}

console.log(calcularSuma(50, 200));
console.log(calcularSuma(500,200));